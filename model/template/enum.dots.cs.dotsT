using DotsSharp.Attribute;
{% for inc in includes %}
#include "{{inc}}"
{% endfor %}

namespace {{defines["NAMESPACE"]}}
{
    public enum {{name}}
    {
{% for elem in items %}
        [Tag({{elem.tag}}, WireName="{{elem.name}}")] {{elem.Name | camel_caselizer }},{% if elem.comment %}// {{elem.comment}}{% endif %}

{% endfor %}
    }
}
